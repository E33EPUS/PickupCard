modLoader = "javafml"
loaderVersion = "[47,)"
license = "MIT"

[[mods]]
modId = "pickupcard"
version = "0.1.0"
displayName = "Pickup Card"
authors = "E33EPUS"
description = '''Every item you pick up is dealt into your HUD as a collectible card: rarity-coloured frame, icon socket, count badge, durability bar and a NEW tag the first time you ever grab that item. Client-side, so it works on any server.'''
# 纯客户端 mod：不参与服务端 mod 表校验，装了就能进任何服务器
displayTest = "IGNORE_ALL_VERSION"

[[dependencies.pickupcard]]
    modId = "forge"
    mandatory = true
    versionRange = "[47,)"
    ordering = "NONE"
    side = "BOTH"

[[dependencies.pickupcard]]
    modId = "minecraft"
    mandatory = true
    versionRange = "[1.20.1,1.21)"
    ordering = "NONE"
    side = "BOTH"
