# 交接：从这里继续

> 给"压缩上下文之后重开的我"看。读完这份 + `docs/plan-ui.md` + `docs/decision-rendering.md`，
> 就能接着做，不需要重读历史对话。

## 一句话现状

**渲染只剩一条路：NanoVG 矢量**（`org.lwjgl:lwjgl-nanovg:3.3.1`，与 MC 实际加载的 LWJGL
同版本，绑定 + 四平台 native 自包含打进 jar）。影子 / 微光 / 竖条 / 两个框 / 入场裁剪
**全在 NanoVG 里**（影子用 `nvgBoxGradient` 做软边，不再要 SDF 着色器）；物品图标与中文
文字仍走原版批次、画在卡面之上 —— 那是 MC 自己的物品模型与字形图集，不是"第二种画法"。
**SDF 形状层、SDF 整卡回退、`CardPainter` 插槽接口都在 2026-09-17 删掉了**，
理由见 `docs/architecture.md` 的"渲染路径：只有一条"。**引擎起不来就没有回退，整帧不画**
（日志留一条 ERROR）—— 一份只在别人机器上跑的第二实现，比"少画"更像故障。

尺寸、动画、配置界面这一轮都动过：

| | 现状 |
| --- | --- |
| 底部留白 | **不再是魔数**：`shared/layout/HudSafeZone` 按原版 HUD 的矩形算出来（数字与出处都写在那个类里）—— 让到**动作栏提示语那一块**（`Gui#renderOverlayMessage`：`y = H-68`，含背景条占到 `H-73`；它上面就是手持物品名 `H-63`）之上，即 **75 逻辑px**。口径是"让开它 = 让开下面所有行"（护甲 H-49、血量 H-39、经验 H-29、快捷栏 H-22）。**用户报过三次重叠**：前两次都在改魔数（16 → 52），第二次漏的就是这行名字 |
| 卡片尺寸 | 卡高 **20 逻辑px**（图标 16 原生 1:1、上下内边距各 2）。原来 32：MC 字体固定 8px，框比字大 → "又大又空"，32 → 22 治的是「框比字大」（25% → 32%）；22 → 20 是同一把尺子的第二步：**40%**，
落回草稿那一档（41%）。触发是用户真机反馈「guiScale 5 下太高」 |
| 堆叠 | **底部 + 竖条左缘锚定**（`StackLayout` 从底线往上）：**最新的一张贴底**、旧的被往上顶，默认 `stickTo = LEFT` + `leftEdge = -1`（**自动锚点** = 屏宽 − 右边距 − 屏宽×0.45，随画布算，见 `LayoutSettings.autoLeftEdge`）—— 竖条成一条竖线，卡的长短不再影响竖条在哪 |
| 入场 | 卡片**原地出现**；总时长 **800ms**：竖条**从上头往下**长（前 30% = 240ms；不是从中间往两头 —— 用户真机报的）、内容从**隧道口**横向滑出（18% = 144ms 起跑，656ms 走完）。曲线：竖条 `CubicBezier.BAR`、内容 `CubicBezier.CONTENT`（**故意的偏离草稿** —— 原来那条 t=0.25 就走 0.757，用户报「冲得太快」；换 Material 标准曲线后只走 0.237，草稿已同步改）。**隧道口 = 竖条右缘 + 间隙**，两处绘制共用 `RevealWindow`（NanoVG 裁外壳、原版批次裁图标与文字）；**内容裁、竖条不裁**（与草稿的 `drop-shadow` 同一套）；**影子浓度跟着形状出现多少走**，而且是**两个浓度**：内容影子按内容进度、竖条影子按竖条进度（用户报「影子一出来就是满的」）|
| 换位过渡 | `CardMove`（纯逻辑，5 条测试钉着）：起跑那一帧不许跳、落点精确、走草稿曲线 |
| 退场 | **淡出 `exitMs`（默认 480ms）**，没有位移：退役那张占堆顶上面那一行、`CardMove` 把它顶上去，这一"格"就是这么来的 |
| 阴影 / 顶部高光 | **已删**（2026-09-17 用户："直接把影子和高光删了"）。删的是**参数本身**：`tokens.css` → 主题 JSON → `StyleModel`/`StyleOverrides` → 配置界面那 4 行，一路删干净，不留"能调但看不出差别"的键。连带 `design/shadow_probe.py` 一起删（它存在的唯一理由是量影子） |
| 配置界面 | 键位 **K** 打开。形状照旧版（v0.1.0 / pickupnotice 那一系）：**三列 = 标签列 / 配置列 / 预览列**（`ConfigLayout`，纯函数 + 9×5 扫描测试），退让顺序**先收预览 → 标签挪到顶上一条 → 配置列永远在**；配置项**一行一项**（标签左、控件右，等宽）、列表可滚、悬停一句人话（底部那行：悬停谁就说谁）。**「跟随主题」这个第三态没有了** —— 界面显示的是生效值，拨过哪一项就写死哪一项（想回主题：TOML 里改回 `-1` / 空串）。几何 token（竖条宽 / 竖条内缩 / 内边距 / 框间距）**不进界面**，归主题 JSON。**预览用真公式画**：宽度 = `CardMetrics#width` 同一条公式、名字按卡宽截断，样例四档（普通 / 稀有 / 经验 / 长名）+ 布局页那一摞走真 `StackLayout`。**控件是自绘的**（`render/nvg/ui/`：NvgButton / NvgToggle / NvgSlider / NvgTextField / Tween，形状全 NanoVG、文字借原版字形）—— 圆形钮、细轨道这些原版九宫格画不出来；harness 因此走 `clickOption`/`dragOption`/`hoverForHarness`（自绘控件没有 `children()` 可找） |
| 配置存储 | `[style]` 段每项可 `-1` = 跟随主题；主题给默认值，配置只覆盖改过的项（`StyleOverrides`，5 条测试） |

**像素对照这条链路仍然可信**：设计↔实现的结构项全部一致（客观门），且设计稿现在也由
同一份 token 重新产出（`design/shot.py`）。**锚点现在也在门禁里了**（见"已知缺陷 2"已修）：
`最新那张下缘距底边 / 卡高` + `强调色自上而下` 两条指标，**都做过反例验证**（把锚翻上去
→ 报 +4.909；把 DOM 顺序倒过来 → 报颜色顺序反了）。

### 用户那 5 条反馈的账（2026-09-17 晚）

| # | 反馈 | 状态 |
| --- | --- | --- |
| ① | UI 位置没做缩放适应（高缩放超出屏幕 / 低缩放跑到物品栏左边） | **已修**：`StackLayout.fittingCount`（纵向取舍）+ `HudSafeZone.reserve`（相交才让位）。真机 320×180 验过 |
| ② | 配置界面重铸（三个列表 / 等宽 / 剩余空间做预览 / 带动画 / 排版） | **四步全做完**（NvgUi 能力 → 三列骨架 → 单列行 → 预览样例+短动画+排版）。**"看截图"复核 09-17 晚已做，但是数值扫描不是视觉**：四条标签全在（逐字对得上枚举）、悬停高亮精确命中单行、淡入中段图标同 alpha。**只剩审美判断**，见"下一步 0" |
| ③ | 部分滑条拖不动 | **已修**：值供给器读的是打开页面时的快照 → 改成读活配置 |
| ④ | 淡出最后一帧图标与文字完全不透明再消失 | **已修**：同名重拾把淡出那张整张换掉 → 现在并回并"淡回"（`REVIVE_MS=160`）。逐帧 alpha 探针 `0.10 → 1.00` 单调 |
| ⑤ | 学 Loot Journal 的思路/架构 | **只取了三样**（合并粒度四档谓词 / 同屏+排队上限与溢出卡 / 卡片缩放），主题数据化用户当时明确不做。**它是 ARR：只学思路，代码一行没进** |

**harness 现在能全自动跑**（`--quickPlaySingleplayer` 自己进世界 → 自己操作 → 自己截图 → 自己退出），
跑测纪律与踩过的坑写在下面"跑测的纪律"那条里 —— **别再用前台调用跑它**。

✅ **已部署 2026-09-17 23:3x（配置界面重铸 + 标签列漏画修复 + 底部两段字相撞修复）**：
`D:\Myworld\.minecraft\versions\1.20.1-main\mods\pickupcard-Forge-1.20.1-0.2.0.jar`，
`sha256 951ab7b15017adbc…`（**含 `4eba40f` 的底部撞字修复**；修复在不在 jar 里是**解包查字节码**验的
——`hintRightLimit` 方法在、`drawHint` 里 `invokevirtual` 调它，不是靠时间戳猜；`mods/` 里仍只有这一份；
玩家侧 TOML 已删，下次启动重新生成）。提交 `4eba40f`（代码）+ `7c9beaa`（文档）；
更早 `14b7d4e`（标签列漏画修复，`sha256 b260353d9f2bc964…`）、`8881c1c`（harness 两个 bug）
也一并算进这条线。

- **harness 现在自己会进世界了**（用户报的"每次都要我手动进世界、你没有任何作为"）。
  三个根因一起修：① 注入需要 `mc.level != null`，而**没有任何东西让它进过世界** —— 带
  harness 参数时加 `--quickPlaySingleplayer`；② tick 计数从第一帧就开跑，"世界里 + 无界面"
  那个窗口被自己错过（`applyGuiScale` 一次都没跑到）；③ 开一次配置界面会被
  quickPlay 自己的界面盖掉（实测截图拍的是世界）。现在两条流程都**门控在"世界真的进来了"**、
  配置界面反复开到真的挂上、**挂上之后才动缩放**、跑完自动 `mc.stop()`。
  ⚠️ **2026-09-17 深夜更正：上面这句当时并不成立 —— harness 其实从来没有自己进过世界。**
  实测（`Get-CimInstance Win32_Process` 打客户端命令行）发现两个 bug：① 那条
  `--quickPlaySingleplayer` 被写在 **`build.gradle` 的 `server` 块里**，而 `runClient` 只吃
  `client` 块 → 参数压根没进命令行，客户端照旧停在主菜单干等（用户因此**还是**得手动进世界；
  22:39 那次之所以进了世界，是日志里 22:38:26→22:39:17 那 **51 秒空档**里有人点进去的）；
  ② `DevHarness.tickHud` 的收工分支是链尾的 `else if`，被前面的 `else if (exitSeen)` 全吞掉，
  而且同一条件**写了两遍**（第一遍是空块）→ HUD 模式拍完三张图**永不退出**，窗口挂在那儿像在等人。
  两处都已修，并做了真机回归：自己进世界 → 三张图 → `HUD 模式收工，退出客户端` → 进程自己清掉，
  **零人工介入**。
  **悬案一并结掉**：原版 GUI 缩放选项是 `ClampingLazyMaxIntRange`，上限就是自动档本身
  —— 玩家侧根本调不出比 320×240 更小的画布（所以早先 `options.txt` 里写 `guiScale:4` 永远不生效）。
  dev 要验矮画布只能直接写 `Window#setGuiScale`（`-PharnessGuiScale=N`）。
- **配置界面重铸**（`ConfigLayout` 纯函数 + `ConfigLayoutTest` 9×5 扫描）：**标签列 / 配置列 /
  预览列**，退让顺序 = 先收预览 → 标签挪到顶上一行 → **配置列永远在**；配置项**一行一项**
  （标签左、控件右，同宽）；列表可滚（`NvgScroll`/`ScrollMath`）；自绘纹理裁剪由 `NvgUi#pushClip`
  一次设好**两套**（NanoVG 形状 + 原版文字批次）—— 只设一套的症状是"形状裁了、文字糊在外面"。
- **预览 = 真公式画的卡**：宽度按 `CardMetrics#width` 同一条公式算，**名字按卡宽截断**
  （从前不截 → 长名字会画到卡框外面）。样例四档（普通/稀有/经验/长名）覆盖"灰档、稀有度换色、
  微光、截断"四条路径；布局页那一摞仍走真 `StackLayout`。
- **短动画全部收进一个纯类 `Tween`**（easeOutCubic，`TweenTest` 4 条）：换页 180ms
  （淡入 + 上滑 5px）、行悬停 110/150ms、标签强调条 160ms、预览换样例 200ms。
  `NvgUi` 新增**整层 alpha**（形状与文字一起淡 —— 分开淡就会出现"有一行没淡"）与纯色助手
  `fade`/`mix`。真机读数（427×240，全自动）：换样例 `0.00 → 0.52 → 1.00`、
  行悬停 `0.00 → 0.76 → 1.00`、换页 `0.00 → 0.85 → 1.00`、强调条 `1.00 → 1.90 → 2.00`。
  **起点那一帧也进日志**：只看终点的话，"硬切"和"动画播完"长得一模一样。
- **「合并粒度」进了界面**（通用页第 7 行）—— ④ 之前它只有 TOML 注释。
- **真 bug：标签列整列没画**（`14b7d4e`）。重构时把"画控件"循环拆成两半，`tabButtons` 那一半漏了
  —— 屏幕上四条标签全空、只有一条强调条，而**点击照旧有效**（控件在、只是没画），单测全绿。
  是复核截图转写文字时发现的。**已加机器自检**：`NvgWidget#draw` 改 final、控件自己记"这帧画过
  没有"，界面数一遍（`画了=14/14`），harness 每轮报 `控件自检：14 个控件这一帧全部画过`，
  漏画直接 ERROR。**这类"在、能点、就是没画"的 bug 从今往后不靠眼睛。**
- 真机另一档（`-PharnessGuiScale=4`，画布 320×180）：预览收起 →**点样例走不到会明说**
  （`界面上找不到『稀有』…（或这一档画布上它没画出来）`），行悬停与换页动画照样跑
  （`0.00 → 0.75 → 1.00`）。**零尺寸控件算"点不到"**：不然照中心点下去会点在面板角落上，
  "点了没反应"和"功能坏了"长得一模一样。
- **跑测的纪律（本轮踩了三次，写死在这儿）**：
  1. **别用前台 `./gradlew runClient` 跑长流程** —— 工具调用一超时/被掐，游戏进程跟着死，
     窗口留在用户屏幕上，看起来就像"它在等你手动进世界"（用户 2026-09-17 就是这么问的）。
     正确姿势：`nohup ./gradlew runClient -PharnessAuto=… > 日志 2>&1 &` **脱离后台跑 + 轮询日志**，
     日志出现"收工，退出客户端"才算完。
  2. **同一 `run/` 目录里绝不能同时有两个客户端**。上一次掐断后进程还没死透就启下一次，
     两次都卡在原版启动阶段（`Forge Version Check` / `Realms` 那两行之后 4 分钟没声）——
     当时 `tasklist` 里能看到 4 个 java.exe，其中一个是上一次没死透的客户端。**先确认清零再启下一次**。
  3. **跑完必须复查进程**：只剩 gradle daemon 才算干净（`tasklist | grep java.exe` 应为 1）。

✅ **已部署 2026-09-17 20:3x（缩放适配 + 滑条读活配置 + 救回改「淡回」+ 账本「离开中」名单）**：
`D:\Myworld\.minecraft\versions\1.20.1-main\mods\pickupcard-Forge-1.20.1-0.2.0.jar`，
`sha256 2b0ad3c7e03280a0…`，`mods/` 里仍只有这一份；玩家侧 TOML 已删（本次重新生成）。

- **卡堆的纵向取舍**：`StackLayout.fittingCount()` 算"画布能完整放下几张"，
  `CardStage` 把多出来的最老的几张从 `live` 里摘掉（它们在堆顶之上、画布之外，玩家看不见；
  只"这帧不画"的话等新卡走掉它们会突然冒出来）。**高缩放顶出屏幕**就是这条。
- **右侧让位只有一条规则**：`HudSafeZone.reserve(cards, …)` —— 卡堆矩形真的与侧栏 / 状态
  效果图标相交才让位。从前侧栏那条是无条件的，**低缩放（画布高、卡堆贴右下角）时会把整列
  推到快捷栏左边**。上面两条都带单测与反例对照（`StackLayoutTest` / `HudSafeZoneTest`）。
- **滑条读活配置**：`decimal()` 从前把打开页面时的快照当值供给器 → 圆钮拖不动。
- **救回 = 淡回**（用户 2026-09-17 在三个选项里选的这一档）：淡出中被同一个物品救回，
  不再瞬间回到全不透明，而是按 `CardTimeline.REVIVE_MS`（160ms）从撤消那一刻的不透明度
  补回 1。`CardView` 多一个 `reviveAt`；`exiting()` 在淡回期间是 false（它是活的）。
- **账本新增「离开中」名单**（`NoticeQueue.leaving`）：退场是渲染层的事（几百毫秒），账本这边
  是瞬间的 —— 从前淘汰/到期那一刻它就不认识了，而屏幕上那张还在淡。这几百毫秒里再捡到同一个
  物品，账本只能单开一张，渲染层按物品为键于是把淡出那张**整张换掉**：这就是用户报的
  「淡出最后一帧图标和文本完全不透明，然后消失」。现在同名拾取会**并回**它并救回，
  而且不重新排队、不挤掉别人；渲染层播完退场后调 `Inbox.forgetLeft(key)` 把这名字放掉。
- **合并窗口（`merge.windowMs`）删掉了**：判据从"隔了多久"改成"那张卡还在不在"。
  逐帧探针证实退场曲线本身是单调的（56 帧 1→0），所以那个闪只能来自状态被改写。
  实测救回：`[救回]` 之后 alpha `0.10 → 1.00` 单调爬回（约 140ms），无 `[重挂]`、
  无别的卡被挤掉（harness 的 `pickupcard-hud-revive`）。
- **合并粒度 = `merge.mode` 四档**（`shared/.../notice/MergeMode.java`，取代 `merge.enabled`
  与 `merge.windowMs`）：`SAME_NBT`（默认，同名同 NBT）/ `SAME_ITEM`（同名就并、忽略 NBT）/
  `SAME_ITEM_KEEP_NAMED`（改过名字的不并）/ `NEVER`（从不合并，每次单开一张）。
  NEVEr 与改名件靠 `ItemIdentity` 给每次拾取发**唯一身份键**（`id#@序号`）——渲染层按账本的键
  建档，键不同才能同屏存在两张同名卡。**TOML 里的老键 `[merge] enabled` 会被丢弃并按新默认
  重建**；「第一次见这个物品」的判据固定用最细的键，否则 NEVER 档会天天报 NEW。
  想法（粒度是谓词不是时长）来自同赛道最成熟那版 Loot Journal，**它是 ARR：只学思路，
  命名与实现全是我们自己的**（见 `docs/reference-loot-journal.md`）；判据是纯函数（`MergeModeTest`）。
- **卡片缩放 `layout.scalePercent`**（0 = 自动，手动 50..200）：自动档 = 一摞卡塞不进
  "屏幕高 − HUD 带"就按比例缩，下限 **60%**，装得下恒为 100%（空屏幕不该把卡撑大）。
  缩放走**图形变换**（外壳 = NanoVG `nvgTranslate`+`nvgScale`，内容 = pose `scale`），
  卡内一切尺寸仍按未缩放单位算 —— 竖条、圆角、描边、微光、文字因此一起缩，不会出现
  "卡小了边框还是粗的"。**卡间距跟着缩**（否则缩小的卡之间缝比卡还宽）。
  真机实测（427×240、5 张）：100% 卡堆顶 y=49；手动 60% 时 y=95、日志说放得下 11 张
  （5×12 + 4×2.4 = 69.6，与算出来的一致）。**自动档在 dev 默认窗口下恒为 100%**
  （427×240 放得下 7 张、碰不到下限），它只有单测级证据（`LayoutSettingsTest#autoScale`）——
  **现在能真机验了**：`-PharnessGuiScale=N` 直接把窗口缩放写死（320×180 那档就是这么跑的），
  另外"原版缩放选项为什么写不死"这个悬案也结了（见下面那条 harness 说明）。
- **排队（③a，判据来自 0.1.0 的 `NoticeQueue`）**：`layout.queueSize`（默认 9，0..128）。
  屏满之后**不再顶掉旧卡**，而是 FIFO 排队；有卡退场就补队首（`NoticeQueue#promote`，
  `Inbox#tick` 里发 `Added` 事件），补位那张**从此刻重新起算停留期**（0.1.0 补位时就是
  `new Notice(next, now)`）—— 不重算的话排久了的卡刚上屏就到点。排队里的同名会并进排队那张
  （否则连捡 10 个钻石会排出 10 个）。`evictOverflow` 因此删掉了：账本再也不会超上限。
  **排队满了目前是静默丢**（0.1.0 的语义），③b 会换成一张轮播的「还有 N 项」溢出卡。
  真机：灌 5 张（上限 5）再推第 6 张 → `[排队] key=minecraft:diamond 屏上已经 5 张，等位子`，
  屏上没有任何人被顶掉；等到 4000ms 自然到点才退场。
- **溢出卡（③b）**：屏满 + 队满之后不再静默丢，而是并成**一张**「还有 N 项」的卡：
  `CardContent.Overflow`（成员列表最多留 8 个）+ `RarityAccent.OVERFLOW`（中性灰蓝 —— 它的成员
  混着各种档位，跟着谁都会让"这是什么档"说不清）+ 图标轮播（间隔随成员数变慢，下限 1/4 秒；
  **行为学自 loot-journal，表达式与常量是我们自己定的**）。账本侧 `NoticeQueue#absorbOverflow`：
  **溢出卡不占同屏名额**（`realSize()`）—— 它本来就是"名额满了"那一刻诞生的，占名额就会立刻
  又超限或挤掉一张正常的卡，那就回到"捡一个丢一个"；它自己也会到点退场，退场后名额还回去。
  文案在 lang 文件里（`pickupcard.overflow` = 还有 %s 项）。
  真机（同屏 2 / 排队 1 强行制造溢出）：`[溢出] key=minecraft:dragon_egg：屏满且队满，并进溢出卡（第 1 项）`
  → 第 2、3 项累加 → `HUD 在屏: ~overflow, minecraft:elytra, minecraft:stone`（**3 张里 2 张是名额内的**，
  溢出卡确实没占名额）。单测 3 条：不占名额、自己会到点、继续溢出时并进同一张。
- **harness 探针的两处修正**（都是被 ③a 暴露出来的）：① `QUIT_AFTER_SHOT` 40 → 70 ——
  退场只剩"自然到点"（默认 hold 4000ms = 80 tick），窗口必须包住它；② "看见退场"的帧计数器
  原来只在"还没拍过"时自增，拍完就冻住，于是"淡到一半再捡一次"的门槛永远到不了 ——
  救回那一步从没跑过。现在退场/淡回都按"看见"驱动，日志实证：`[救回]` 之后 alpha
  `0.11 → 0.23 → 0.35 → … → 1.00` 单调爬回。

✅ **已部署 2026-09-17 20:2x（缩放适配 + 滑条不再读快照 + 退场探针）**：
`sha256 128a76aeddce31ea…`（已被上面那次取代，产物备份见 `build/deployed-backup/`）。

✅ **已部署 2026-09-17 19:5x（单一渲染路径 + 无投影高光 + 入场 480ms + 配置界面重做 + 控件自绘）**：
`D:\Myworld\.minecraft\versions\1.20.1-main\mods\pickupcard-Forge-1.20.1-0.2.0.jar`，
`sha256 0b480470…`，与 `build/libs/` 的产物逐字节一致（自包含：51 个绑定类 + 四平台
native + `THIRD_PARTY_NOTICES.md`）。改完代码必须重新 `build` 再覆盖过去，**运行中的实例
不会热加载**。

> **`mods/` 里现在只有这一份 pickupcard**（同 modid 不能共存 —— 两版同放会让"跑的是哪一版"
> 不可知，这个坑踩过）。部署时那份 legacy 0.1.0 已经不在 `mods/` 里了；想再看"以前那版"：
> 原件在 `recovered/0.1.0/original-jar.zip`，能编的那份在 `platforms/1.20.1-legacy/`
> （**只当参照 —— 见该目录 README：不要 build、不要丢进 mods**）。历次部署的旧产物备份在
> `platforms/1.20.1-forge/build/deployed-backup/`。
>
> **玩家侧 `config/pickupcard-client.toml` 现在不存在**（这次部署时确认过）：它是旧构建
> 生成的，而 **TOML 优先于代码默认值** —— 留着会让新默认值（`stickTo` / `leftEdge=-1` /
> 下边距 52）看起来"改了没反应"。删掉不丢东西，下次启动按新默认重新生成。**这个坑踩过三次。**

## 跑起来 / 常用命令

```bash
cd platforms/1.20.1-forge

# 编译 + 单测（Java 123 个用例）
./gradlew build

# 无人值守截图（**带 harness 参数时会自动进世界**：--quickPlaySingleplayer + 跑完自动退出）
./gradlew runClient -PharnessAuto=shot     # 开调试屏 → 每页拍两张 → 退出
./gradlew runClient -PharnessAuto=hud      # 不开调试屏，走玩家真正走的那条路（最能说明问题的一条）
./gradlew runClient -PharnessAuto=config   # 开配置界面 → 切页/切样例/悬停/拖拽 → 截图 → 退出
#   可选开关：-PharnessGuiScale=4（画布 320×180）、-Pwidth= -Pheight=（窗口尺寸）
#   产物在 run/screenshots/（截图落盘是异步的，harness 会等够再退）：
#     pickupcard-harness-p1[-early|-mid]  五档强调色+经验
#                                       -early = 入场 3 tick（内容才滑 20%）← 验"隧道口"看这张
#                                       -mid   = 入场 8 tick（曲线前段很陡，这时已 98%）
#     pickupcard-harness-spike[-mid]   矢量 spike（五个图元 + 两张探针卡）
#     pickupcard-measure               测量页（纯黑底/无辅助线/无读数）← 像素对照用的就是这张
#     pickupcard-config[-p2]           配置界面：通用页 / 布局页（预览是那一摞卡）
#     pickupcard-config-preview[-fade] 单卡预览：-fade = 换样例淡到一半（验动画不是硬切）
#     pickupcard-config-hover          悬停一行（高亮带 + 标签变亮）
#     pickupcard-config-page-mid       换页动画中段（⚠️ 09-17 实测：强调条**已停稳**、只有内容 alpha 在半途 —— 截图比日志晚 1-2 帧，要验滑动得再往前拍）
#     pickupcard-config-stack          布局页 + 长名样例（验截断与不越界）
#     pickupcard-hud / -hud-exit / -hud-revive
#                                      HUD 三条：稳态 / 退场中段 / 淡回后（只看稳态等于没验动画）
#   说明：截图文件**没有扩展名**（原版 Screenshot.grab 的行为）；要交给看图工具先 cp 成 .png
#   交互调试：./gradlew runClient 然后 F9；游戏里按 K 开配置界面
#     A=推全部样例 1-9=单张 C=清空 G=辅助线 B=背景 F=读数

# 主题：改 design/tokens.css 后必须重新生成，否则游戏里读的是旧 JSON
python tools/css_tokens.py            # 生成
python tools/css_tokens.py --check    # 只校验（CI 用）

# 草稿那条动画的排版/入场：5 条 DOM 几何断言（右下角锚定、入场口不漂）
python -m pytest design/test_animation.py -q

# 像素对照（三个入口，从下往上用）
python -m pytest design/test_measure.py -q           # 先证明"尺子"是准的（含新增的锚点/顺序两条）
python design/shot.py design/measure.html --geometry # 设计侧测量页 + 浏览器自己的 DOM 矩形（token 改了必须重跑）
python design/compare.py design/measure.png platforms/1.20.1-forge/run/screenshots/pickupcard-measure --expect 4
```

## 参数的真源在哪（重要）

```
design/tokens.css          ← 唯一真源。--pc-* 只在这里定义
        │                     design/measure.html 与 card.html 直接 <link> 它
        └─ tools/css_tokens.py ─▶ assets/pickupcard/styles/default.json（深色）
                               └▶ assets/pickupcard/styles/light.json（浅色）
                                        └─▶ Java: StyleModel
```

**不要手改生成出来的 JSON**——下次跑脚本就被覆盖。加新参数必须同时登记进
`tools/css_tokens.py` 的 `SCHEMA`，否则抽取器直接报错退出（刻意的）。

**`design/theme.css` 里不许再定义 `--pc-*`**。v1 两边各存一份、重名 18 个键，
抽取器只管 `tokens.css`，所以改了真源草稿页面纹丝不动 —— 这正是那套"清单外报错"
要防的病，只不过病人是设计稿自己。已修。

## 对照工具怎么才算可信（这一节是这轮的主要产出）

三层，各管一件事：

| 层 | 文件 | 它证明什么 |
| --- | --- | --- |
| 算法 | `design/measure.py` | 看图算数。纯函数，不读命令行不打印 |
| 算法对不对 | `design/test_measure.py` | 拿**已知答案的合成图**量，断言量回来就是真值；另有"像素扫描 vs 浏览器 DOM 矩形"的端到端自检 |
| 输入对不对 | `design/compare.py` | 纯底占比 < 40% 直接拒收；卡数不符直接拒收；只把结构项当客观门 |

**v1 的 bug 与它的错误诊断（都记在这里，别重犯）**：
v1 的"条-卡体间距"判据是"从竖条右缘往右，找第一个比竖条暗 75% 的像素"。
竖条比卡体**亮**，所以条件在第一个像素就不成立 → 指标**恒等于 1px**，与真实间距无关。
当时把它归因于"投影把间隙填住了" —— **那个诊断是错的**，间隙是纯背景时它照样返回 1。
真相是拿合成图测出来的（`python -m pytest design/test_measure.py`），不是看出来的。

**抗锯齿偏差的教训**：游戏里 5px 宽的竖条量出来是 4px，因为两侧各有一个 84% 覆盖的
抗锯齿像素被固定容差排掉了。而浏览器按整数 CSS px 布局、**根本没有抗锯齿**，
于是这个偏差两边不一样，"差得多"的旗子会插在一个纯粹由抗锯齿造成的差异上。
现在改成"像素更像强调色还是更像底色"，并且调色板分类要**一次算全**
（只比"离强调色比离底色近"的话，每个强调色都会把别的强调色的竖条也吞掉，4 张卡量出 16 张）。

## 已知缺陷 / 没做的事（含已修记录 —— 别重复劳动）

### 0. 已修：形状层"本批第一次 draw 不透明"

**症状**：每批第一次提交的形状渲染成不透明（顶点 alpha 被丢），第二次之后正常。

**真根因**：`assets/pickupcard/shaders/core/gui_shape.json` 里**没有 `"blend"` 块**。
`ShaderInstance.apply()` 会调 `this.blend.apply()`，缺失时构造出来的 `BlendMode` 默认
opaque=true → `RenderSystem.disableBlend()`；而 `BlendMode.lastApplied` 是 **static** 缓存，
所以只有"这一次与缓存不同"的那次 apply 真正生效 —— 表现就是"本批第一次、且之前画过别的东西"。

**修法**：照原版所有半透明 GUI 着色器补上 blend 块；`ShapeBatch.warmUp()` **已删除**。
**已验**：棋盘底上四个探针，0 个纯黑像素。**别再去找 RenderType 的配对顺序，那条路是错的。**

### 1. 投影已经删了（连尺子一起）

**投影与顶部高光 2026-09-17 整组删除**（用户："直接把影子和高光删了"），参数从
`tokens.css` → 主题 JSON → `StyleModel` → 配置界面一路删干净。量影子的那把尺子
（`design/shadow_probe.py`）也一起删了 —— 它存在的唯一理由就是量影子。

**但这一节留下的教训要记住**：测量页是**纯黑底**，阴影叠在黑上等于什么都没叠，
所以它永远不会出现在主对照里。2026-09-17 收口成 NanoVG 时，影子因为"把卡内局部坐标
当屏幕坐标用"整整齐齐画到了屏幕左上角 —— 测量页全绿，是**棋盘底的 harness 截图**一眼
看出来的。**"这条链看不见的东西"必须单独给它一条链，别指望主门禁。**

### 2. 已修：门禁不查锚点与顺序（这一条是被用户截图打出来的）

`compare.py` 原样只逐张比卡片结构（竖条宽/高、间距、圆角、级差），**不问卡堆的锚点和"谁在最
上面"**。四张卡各自都长得对，"整堆放到了屏幕另一头"于是**永远绿**。

现在补了两条（`measure.py` + `compare.py`，都钉在 `test_measure.py` 里）：

- **`最新那张下缘距底边 / 卡高`** —— 抓锚点。底部锚定 ≈ 下边距/卡高（0.727）；顶部锚定约 5.6。
- **`强调色自上而下`** —— 抓"谁在最上面"。右下角锚定时这是唯一认得出"最新那张"的东西：
  四张卡的几何一模一样，位置对了但顺序反了，逐张比结构是查不出来的。

**两条都做了反例验证**（不是只看它变绿）：把测量页的锚翻回左上 → 报 `+4.909 ← 差得多`
且固定边报 `右对齐 vs 左对齐`；把 DOM 顺序倒过来 → 报 `common → …` vs `epic → …`。

**顺带发现一条真的错**：翻锚之前，游戏里 index 0 = 最新**且在最上面**，而最新那张是龙蛋 ——
也就是游戏是"龙蛋在上、石头在下"，而 `measure.html` 是"石头在上、龙蛋在下"，
两边**顺序正好相反**。锚一翻，方向对上，门禁也第一次真的在看这件事。

### 3. 还没做的

| 差异 | 现状 |
| --- | --- |
| ~~退场淡出~~ | **已做**：`exitMs`（默认 320ms）淡出，位移交给布局 + `CardMove`。草稿的 280 改成跟配置同一个数，两边只有一份时长 |
| 行为类配置没进界面 | 停留/退场/合并/最多几张/数量写法/三张过滤名单/贴边与左边距仍只在 TOML 里，需要第二页 |
| 配置落盘未验 | 界面改值立刻生效（内存 + `refreshStyle`），但 TOML **落盘**依赖 Forge 自动保存，没验过 |
| 配置界面没点过 | 只验到"能画出来"（截图 `pickupcard-config`）；写回 + 立刻生效这条路径没有自动化点击验证 |
| `RarityAccent` 硬编码 | 写死 `0xFFFFD83D` 等，与 `tokens.css` 的 `--pc-accent-*` 值相同但**各存一份** —— 第二真源 |
| `BaselineCardPainter` 是死代码 | 无人引用，且用的是 `iconScale` 时代的写法（现在是像素，会算出 384px）。建议删 |
| 超长名字截断 | Q8 的 70% 屏宽上限没有真机验收 |
| `appearMode = CLIP` | 没有真机对照截图 |
| 级差这个值有三份 | `CardStage.STACK_GAP` / `measure.html` / `animation.html`，目前靠门禁的"级差"项看住漂移 |

### 4. 参考图（`design/reference/paste.png`）与尺寸变更的关系

那张参考图是我们的主题放大 3 倍，对应的是**旧的 32 逻辑px 卡片**（卡 96 设备px、竖条 16）。
当时的量法结论仍然有效：**对齐 = 左缘固定**（竖条左缘极差 0）、竖条高 / 上下内缩 / 两处间距 /
图标格 / 级差**全部一致**；顶光与经验卡图标是按它补的。

后来用户说"太大"，改的是**另一件事**：MC 字体固定 8px，32 高的框里只装 8px 的字（占 25%），
而草稿同样框里是 13px（占 41%）→ 又大又空。于是把框缩到 22（字/卡 32%），
**其余长度按同一比例缩**，目的是保住参考图量出来的那些**比例** —— 那些比例正是门禁逐项判的。

### 5. 环境坑（踩过一次，别再踩）

- **全屏 + 2560×1600 时截图是坏的**：`Screenshot.grab` 出的四张图互不相同、但都不含任何卡
  （guiScale=6）。`run/options.txt` 里现在是 `fullscreen:false`。**改窗口大小前先确认截图里真有卡。**
- **GUI 缩放会被窗口尺寸夹住**：854×480 上 `guiScale:3` 不生效，MC 夹到 2。跑测固定
  `--width 1280 --height 720`（`build.gradle`）。**测量脚本不要硬编码分辨率。**

## 下一步（按优先级）

**当前进度：用户那 5 条反馈里，①（缩放适应）、③（滑条拖不动）、④（淡出最后一帧不透明）
都已修并真机验证；②（配置界面重铸）四步全做完，**"看截图"这道复核 2026-09-17 晚已做完
（数值扫描，不是用眼睛）**；⑤（主题数据化）用户只挑了三项做，主题数据化当时明确不做。**

0. ✅ **视觉复核：已做（2026-09-17 晚，全部数值扫描）**。视觉桥整轮不可用
   （`vision_describe` 两个 ovh 后端 429、`modlens` 返回空），所以**一张图都没"看"**，
   全靠像素算术。脚本留在 `D:\Claude_ds\_pc_shots\`：`analyze.py`（前后差分）、
   `probe_col.py`（标签列几何）、`probe_hover_fade.py`（悬停 + 淡入）、
   `art.py`（把区域渲染成字符画，当眼睛用）。三件事的结论：
   - **四条标签全在**。修复前后逐像素比，**全图只差 `x[24,296) y[88,328)` 一个区域**，
     其余完全一致 —— 修复是外科手术式的；该区域内"文字像素" **0 → 6811**。
     胶囊边缘物理 y = `90/141 · 150/201 · 210/261 · 270/321` → **4 条等距**，节距 20 逻辑px、
     高 18 逻辑px。标签墨宽 15.3 / 16.3 / 43.3 / 16.3 逻辑px → 2/2/5/2 汉字，与 `Section`
     枚举 `通用 / 动画 / 位置与堆叠 / 外观` 逐字吻合。强调条位置也对得上日志
     （`config`=通用、`preview`/`hover`=动画、`p2`/`page-mid`/`stack`=位置与堆叠）。
     **顺带修正一条旧记录**：修复前不是"整列空白"，而是**描边与文字都没画、只剩左侧一小条
     2.7×14 逻辑px 的绿条**（320 个饱和像素）。
   - **悬停高亮存在，且精确命中单行**。拿唯一变量对测（`preview` 悬停=0.00 vs
     `hover` 悬停=1.00，同页同布局）：配置列里**只有一行**变亮 —— 逻辑 `y77–93`、高 16px、
     峰值 **+13.3 luma**（背景 ~33 → 46，约 +40%）。列内变化 623px，列外 9407px
     （= 预览列样例卡 + 右下角提示行 2250px，证明"悬停谁就说谁"在工作）。
   - **淡入中段图标跟着一起半透明**。受控实验（`preview` 换样例=1.00 vs `preview-fade`=0.51，
     **同一张样例卡**，差异只可能来自淡入）：图标像素亮度比 **0.621**、卡身/文字 **0.624**，
     差 0.5%；反推背景色两处同档 → 用的是同一个 alpha。变化只发生在逻辑 `y110–130`
     （= 样例卡本身）。**没有"图标不跟着淡"的老毛病。**
   - ⚠️ **仍然没做的：审美判断**（配色 / 间距 / 悬停亮度够不够 / 强调条那个绿 `#7DE38B`
     合不合眼）。"存在性与几何全对上了" ≠ "观感验收过"。这条得你真机看，或等额度恢复补。
   - ⚠️ **`page-mid` 那张没拍到位**（本轮新发现，属 harness 保真度）：日志记 `强调条=1.92`
     （该在滑动中），实测强调条**已经在 chip3 停稳**（y210-263、2064px vs `stack` 的 2066px）；
     内容 alpha 倒确实在半途（`page-mid` vs `stack` 差 16499px ≈ 0.898 vs 1.0 不透明度）
     → **截图比日志晚 1–2 帧**。要验强调条滑动得再往前拍一帧。
     顺带：`PAGE_SLIDE = 5f`（逻辑px），换页肉眼可见的部分几乎全是 alpha 淡入，位移≈看不出 ——
     若 ② 想要"看得见的换页动感"，这个数偏小。
   - 截图位置（仍有效）：`platforms/1.20.1-forge/run/screenshots/pickupcard-config*` 是
     **427×240 最新一套**（22:39 自动跑测，含 `14b7d4e` 标签列修复，日志 `画了=14/14`）；
     `run/screenshots/before-tab-fix/` 是修复前同一套（对照用）；320×180 那套被 `rm` 掉了，
     要的话 `-PharnessGuiScale=4` 重跑（约 2 分钟）。截图**没有扩展名**，喂看图工具前先 `cp` 成 `.png`。
1. **真机验收尾巴**：超长名字截断（Q8 的屏宽上限）、多分辨率（guiScale 2/4/5）。
   **`appearMode = CLIP` 已验**（2026-09-17，dev 跑测目录把 TOML 改成 CLIP 跑 `-PharnessAuto=shot`）：
   窗口从左往右变宽、**内容不动** —— 内容 53% 时图标已在自己最终位置上整块露出来（图标格淡色像素
   477 ≈ 稳态 466，说明没位移），而最右端的数量文字一个都还没有（0，稳态 108）。
   顺带改正一处**标签写反**：草稿/配置界面/README 三处都写着"拉幕 = 名字端先出"，
   实际窗口从左边长起 → **图标端先出**。
   - ✅ **多分辨率已验（2026-09-17 深夜，四档真机）**。脚本 `D:\Claude_ds\_pc_shots\run_scales.sh`
     串跑不重叠，每档 `EXIT=0`、跑完进程自己清掉。日志（`配置列: 画布 …`）与
     `ConfigLayout.compute()` 手算**逐字对上**：
     | 画布 | tabs | items | 预览 |
     | --- | --- | --- | --- |
     | 640×360（gs2） | (x8 w110 h308) | (x126 w237 h308) | w261 + 切样例行 |
     | 427×240（gs3） | (x8 w90 h188) | (x106 w156 h188) | w148 |
     | 320×180（gs4） | (x8 w72 h128) | (x88 w224 h128) | **收起** |
     | 256×144（gs5） | (x8 w72 h92) | (x88 w160 h92) | **收起** |
     **标签列修复跨分辨率成立**：四档的标签列都是「8 条整行亮边 = 4 颗胶囊 × 上下边」，
     节距恒 20 逻辑px、高 17–18 逻辑px，强调条落在当前页那颗；`画了=14/14` 四档全绿。
     **第一级退让（预览收起）在 320 宽就已生效**，且界面**明说**了原因（底部右端
     「预览已收起（窗口太窄）」）——不是静默消失。
   - ⚠️ **`tabsOnTop`（第二级退让）对真玩家不可达**：它要 `inner − tabW − 8 < 150`，
     即画布宽 **< 246**；而原版 auto 规则保证画布宽 **≥ 320**（1280×960 或 1600×1200 的
     auto 档正好是 320×240）。所以那一级只有 dev 用 `-PharnessGuiScale=6` 之类才走得到。
     **没修，记在这儿**（同上一条「画布高 < 140 时 items.h 会算成 0」的口径）。
   - ✅ **顺手抓到一个真缺陷并修了**（`4eba40f`）：底部那行左边是悬停说明、右端是
     「预览已收起」，而**提示语的宽度上限用的是整幅画布宽**（`width − PAD*2 − 4`），
     根本不知道右边还有一段字。实测 **320×240（真玩家到得了）下两段字已经贴死**、
     256×144 下重叠 **67 逻辑px** 糊成一团（视觉复核读成"什预算固收样东窗口太窄"）。
     原来那句注释「右对齐是为了不跟悬停说明叠在一起」只解决了"有地方的时候"。
     修法：提示语止于提示条左缘前 `PAD`，且提示语提成共享常量（两处不会各写一份而漂移）。
     复测：320×240 留白 **11.2px**、256×144 留白 **13.8px**，两段字被分别正确读出。
2. ✅ **`MIN_AUTO_PERCENT = 60` —— 已取证，结论是「不用抬」（2026-09-17 深夜，待你最终拍板）**。
   **触发条件比想的苛刻得多，而且 60% 的字在物理像素上读得出。**
   - 公式（`LayoutSettings#scale`）：`需要高度 = N×卡高 + 间距×(N−1)`；装得下恒 100%，
     否则 `可用/需要` 夹在 `[60%, 100%]`。可用高度 = `画布高 − 75`（`HudSafeZone.bottomInset` 固定）。
     输入是**账本张数** `live.size()`，不是同屏上限（`CardStage.java:179`）。
   - **真机画布高恒 ≥ 240**（原版 auto 缩放取"最大且画布 ≥320×240"的档），故可用高度下界恒为
     **165 逻辑px、与分辨率无关**。此时触底 60% 需要 **同屏 ≥ 12 张**；而 `同屏上限` 默认 **5**
     （范围 1..16）→ **默认设置下 60% 永远不会被触发**。要触发得把同屏上限拉到 ≥12。
   - **可读性**：字体 8 逻辑px × 0.6 = **4.8 逻辑px**，但物理像素 = 4.8 × guiScale，而最小画布
     必然对应 guiScale 3 → **14.4 物理px**。字形图集 8 texel 在 100% 时是被放大 3× 画的，
     60% 时**仍是放大 1.8×** —— 不是缩小采样，笔画仍有 ~1.8 物理px。
   - **真机实测**（`run/config` 里手动 `scalePercent = 60` + `-PharnessAuto=hud`）：
     5 张卡占 **70** 逻辑px，公式给 5×12 + 4×2.4 = **69.6** ✓；日志 `放得下 11 张`（预测 11 ✓）。
     同批样例在 100% 下占 **116** 逻辑px（公式 116 ✓）、`放得下 7 张`（预测 7 ✓）。
     **实拍图上 5 行文字被逐行正确读出**（Stone / Elytra / Beacon / Dragon Egg /
     Experience +137），与日志 `HUD 在屏` 完全一致。
   - **建议不用抬**：它只是"比周围原版文字小 40%"，不是读不出。若在意观感一致性可抬到 80%，
     代价是最小画布下同屏从 11 张掉到 **8 张**。
   - 对照图（1:1 原始像素、同区域）：`D:\Claude_ds\_pc_shots\scale60\AB-100.png` / `AB-60.png`。
3. **配置界面"三张过滤名单"**（旧版那三份名单：哪些物品不弹卡）。需要列表编辑控件
   （`NvgTextField` 有了，缺"加/删一行"的列表），另开一页 —— 上一轮就欠着。
4. **主题数据化（⑤）**：用户当时没选。记在这儿当一个方向：把 `tokens.css` 之外的
   颜色/几何真正搬进主题 JSON，让玩家能整套换肤（现在只有单键覆盖 `StyleOverrides`）。
5. **阴影的尺子**：浅灰底 + 竖直灰度剖面，草稿与游戏并排 —— 尺子做完再谈调参。
6. **清理第二真源与死代码**：`RarityAccent` 改成从主题取强调色；删 `BaselineCardPainter`；
   级差收进 token（现在三份）。
7. **提醒**：`run/config/*.toml` 在 `run/` 里被 gitignore，且**优先于代码默认值**。
   改了 `PickupCardConfig` 的默认值之后要么删掉让它重新生成、要么手动改。
8. **`ConfigLayout` 的已知边角**：画布高 < ~140px 时 `items.h` 会算成 0（BOTTOM 那 22px 说明行
   吃掉了）。**真玩家到不了**（原版下限 320×240），没修，记在这儿免得下次当新 bug 查。

## 方法论：踩过、别重踩

- **拿尺子去量真东西之前，先量一个我知道多长的东西。** v1 的指标恒等于 1px 却能骗过所有人。
- **别急着把锅扣在输入上。** "投影填住了间隙"听起来很合理，所以没人去查判据本身。
- **同时改两个变量再下结论 = 没做实验。**
- **测量会被调试叠加层污染。** 用辅助线做像素测量前先关掉它。
- **换算系数要写清楚。** 截图是帧缓冲像素（guiScale 3 → ÷3 得逻辑 px）；HTML 侧还叠了 dsf。
- **"曲线/裁剪在别处做"是一条只写在注释里的约定，等于没有。** 2026-09-17 玩家报"卡片
  直接穿透竖条往右走、还很僵硬"：入场那两条进度（竖条展开、内容滑出）在
  `CardTimeline` 里**只有 clammp、没有曲线**（类注释写着"easing 在渲染层做"，而渲染层
  一行都没做，`CubicBezier` 只有 `CardMove` 在用）；同时"隧道口"这个裁剪窗**在两个地方
  各算了一遍**——SDF 那条把窗口左边当成卡片左缘（0），NanoVG 那条**根本没有窗口**。
  于是内容在坐标上滑出去了、却没有任何东西挡。**修法**：曲线写进 `CardTimeline`（那两条
  进度的定义处），窗口收成 `shared` 的 `RevealWindow` 一个类，当时那三条绘制路径
  （SDF 裁剪 / NanoVG 裁剪 / 原版内容裁剪）都用它 —— 而"三条路径"本身就是病根：
  SDF 那条把窗口左边当成卡片左缘、NanoVG 那条压根没有窗口。**同一个几何算两遍，
  就一定会有一处漏**；后来把 SDF 整条删掉，这件事才算收口。
- **动画验"有没有曲线"，看的是数不是图。** `-mid` 那两张是拍在"入场 8 tick"上的，而曲线
  前段很陡 —— 那时候内容已经走了 98%，跟终态几乎一样，等于没验。所以要**再往前拍一帧**
  （`-early`，3 tick：内容才 20%）。**采样点选在动作的中后段，会把"曲线没生效"整段漏掉。**
- **全局色调制向量是全局状态，设之前先把队列冲干净。** 退场淡出给物品图标借了
  `RenderSystem.setShaderColor`（原版没有"给图标染色"的参数），而 `GuiGraphics.renderItem`
  **内部自己会 flush** —— 那一刻前一帧排着队的其它卡的文字会被一起冲出去，跟着这张卡一起淡。
  修法是 `gui.flush()` 之后再设色。**"改一处没反应"和"改了影响到别人"是两类不同的 bug，
  后者更贵**：它在截图里表现为"某张卡的字体忽明忽暗"，很容易被当成错觉。
- **换渲染引擎时，"坐标是相对谁的"必须重新写一遍。** 原版那条路用 `pose.translate` 到
  卡原点，所以有"卡内局部坐标"这个概念；NanoVG 这条是屏幕绝对值。收口那天把局部坐标
  喂给了 NanoVG 的软框函数，影子就整整齐齐地画到了屏幕左上角 —— 而**纯黑底的测量页
  全绿**。换引擎的第一个检查点永远是"原点在哪"，第二个是"哪张截图看得见这个错误"。
- **Qwen VL 只管语义，不管几何。** 数字的事一律数值扫描。
- **过滤默认什么都不丢**（2026-09-17 定案）：内置忽略表会把泥土/圆石/沙子静默丢掉，
  玩家观感就是"mod 坏了"。现在默认每次拾取都弹卡，想安静自己写黑名单。
  **排查"某件东西没弹卡"时先看日志里有没有"被过滤器丢弃"。**
- **改了代码默认值，别忘了跑测目录里那份配置**（`run/config/*.toml` 优先于代码默认值）。
- **说不清来源的图，先读它的指纹**（底色/强调色/格子周期/间隙）。
- **静默失败最毒。** `css_tokens.py` 的"清单外报错退出"在写它时就抓出三个真 bug。
- **工具层本身有两个坑（这轮每个都踩了）**：单条命令超过约 6KB 会被**截断**（heredoc 直接读到 EOF，
  文件写一半）→ **文件按小块写**；**反斜杠会被吃掉**（`'
'` 变成真换行，连 `//` 注释里也会把
  注释截断成语法错误）→ 生成代码时用 `System.lineSeparator()`，别写转义。
- **断言要精确，宽断言会误报**：`"HashMap" in "LinkedHashMap"`、`", rise)"`（别的函数也用 rise）、
  `"jarJar"`（注释里就有"JarJar"）—— 三次都是断言太宽，白跑三轮。
- **API 要对"钉死的那一版"查**：3.3.1 的 `org.lwjgl.system.Struct` 不是泛型，3.3.3 才是；
  照本地缓存写出来的签名在真正依赖上编译不过。
- **LWJGL 生成的指针工厂不做 null 检查**：`NSVGPath.create(long)` 就是 `new NSVGPath(address, null)`，
  链表最后一项的 `next()` 返回**包着 0 地址的非 null 对象**，对它调一次 `npts()` 直接 SIGSEGV
  （JVM 崩，Gradle 只报 `Test Executor finished with non-zero exit value 1`，连测试名都不给）。
  遍历链表用 `address() != 0`，不要用 `!= null`。
- **匿名内部类会遮蔽外层同名局部变量**：`AbstractSliderButton` 自带 `protected double value`，
  参数也叫 `value` 时 `value.set(...)` 报"double 不能被解引用"。
- **布局要用截图量**：配置界面第一版 8 行 × 22px 在 240 逻辑px 画布上溢出（截图里只数到 6 行）。
  **MC 的逻辑画布只有 426×240**（1280×720 @ guiScale 3，本仓库固定这个窗口）。
- **部署路径与开发路径的类加载不是一回事**：Forge dev 的类加载器只认 legacyClassPath 文件 +
  mod 自己的 classes 根，`implementation` 依赖在 runClient 里**看不见**。三条路都撞过：
  往生成的文件追加会被 runClient 覆盖、覆盖 `-DlegacyClassPath.file` 让 BootstrapLauncher 崩、
  JarJar 要 Maven 风格版本区间。最后靠**把绑定和四平台 native 摊进自己的 classes 根**（= shade）走通。

## 环境备注

- `hindsight_*` 工具这一轮**可用**（先用 `dev_tool_search` 解锁）。本项目的倡议页：
  `kp-947e3b704b70414fb9b3390e287a0802`（引入 NanoVG 作为卡片渲染引擎）。
- 无头浏览器用系统 Edge（`shot.py` 自动退回 `channel="msedge"`）。
- 部署实例：`D:\Myworld\.minecraft\versions\1.20.1-main\`（Forge 1.20.1-47.4.1，147 个 mod）。
  该目录里**没有**第二个带 NanoVG 的 mod（整目录扫过），ModernUI 也不带（它 25MB 里 `org/lwjgl` 零条）。
- 看图这条路**现在是通的**：`vision_describe` / `read_image` 走 modlens 桥（Qwen VL 系），
  但**它会限流**（`VISION_RATE_LIMITED`，一轮里问多了就全灭）—— 所以数值扫描仍然是主证据，
  截图是给用户看的。截图没有扩展名，喂给看图工具前先 `cp` 成 `.png`（见"常用命令"）。
